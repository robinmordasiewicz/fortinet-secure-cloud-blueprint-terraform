name: Branch Deleted
on:
  - delete
jobs:
  create:
    if: github.event.ref_type == 'branch'
    runs-on: ubuntu-latest
    steps:
      - name: Branch Deleted
        run: |
          gh workflow run tf-plan-apply.yml --ref main -R robinmordasiewicz/multi-environment-controller
